#!/usr/bin/env bash
#bash script that configure server with some requirements

sudo apt update
sudo apt install nginx -y
echo "Hello World!" | sudo tee /var/www/html/index.html > /dev/null
sudo service nginx reload
